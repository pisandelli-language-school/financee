// DareDash icon overrides. Project icon set: Lucide (@iconify-json/lucide).
// See docs/handoffs — iconography is Lucide-based.
export default defineAppConfig({
  daredash: {
    icons: {
      modalClose: 'lucide:x',
      selectArrow: 'lucide:chevron-down',
    },
  },
})
